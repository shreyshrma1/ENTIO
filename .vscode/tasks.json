{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile Entio extension",
      "type": "shell",
      "command": "npm run compile",
      "options": {
        "cwd": "${workspaceFolder}/vscode-extension"
      },
      "problemMatcher": ["$tsc"]
    }
  ]
}
